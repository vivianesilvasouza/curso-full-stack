let spacialStation = {
    name: 'receptora',
    parseFloatFormQuantity: 15,
    shape: 'Esférica'
}

/* console.log(spacialStation.name.toUpperCase()) */

let spaceshipName = ['fenix', 'puller', 'golias']

console.log(spaceshipName[0].toUpperCase())