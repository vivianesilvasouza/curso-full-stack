/* let spaceShip = {
    name: 'superNova',
    type: 'batalha',
    crew: ['"cap.Viviane", "Ana","bruna"']
}

spaceShip.crew.push("ten.Fernanda") //para acrescentar

console.log(spaceShip) */
/* 
let spaceShip = [
    { name: 'elemental', crewQuantity: 10 },
    { name: 'colossus', crewQuantity: 8 },
    { name: 'helmet', crewQuantity: 15 }
]

 console.log(spaceShip[0].name) 

spaceShip.forEach(spaceShip => {
    alert(spaceShip.name + 'tem' + spaceShip.crewQuantity + 'tripulantes.')
})

 */

let spaceShip = {
    name: 'golias',
    max: 20,
    captain: {
        name: 'hugo',
        age: '37'
    }
}

console.log(spaceShip.captain.name)