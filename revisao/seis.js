/* let nomeNave = prompt('qual nome da sua nave?')
let subistituir = prompt('qual caractere deseja substituir')
let trocar = prompt('por qual caractere deseja substituir?')
let nova = ''

for (let indice = 0; indice < nomeNave.length; indice++) {
    if (nomeNave[indice] == subistituir) {
        nova += trocar
    } else {
        nova += nomeNave[indice]
    }
} */

for (let i = 1; i <= 5; i++) { //(começo; condição; execução)
    console.log('estou aprendendo!', i)
}