/* function greepilot() {
    alert('bom dia!')
}

greepilot() */

function speeUp(velocity, aceleration) {
    let newVelocity = velocity + aceleration
    console.log('nova velocidade:' + newVelocity)
}

speeUp(60, 10)//os parametros sempre tem que ser chamado na mesma ordem que foi declarada na função.